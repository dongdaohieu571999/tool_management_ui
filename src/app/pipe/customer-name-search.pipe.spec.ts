import { CustomerNameSearchPipe } from './customer-name-search.pipe';

describe('CustomerNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
